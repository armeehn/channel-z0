{
  "name": "channel-z0",
  "private": true,
  "type": "module",
  "description": "Channel Z0 — the storefront Worker. The station itself is not a node project.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node tools/test-all.mjs",
    "test:relay": "node tools/test-comment-relay.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "wrangler": "^4.125.0"
  }
}
